# app/eval/

The three-arm baseline comparison (adaptive UI vs. chat-only vs. static
worked-example) plus the benchmark/transfer harness that drives the personas
through the tutor and reports the honest numbers.

These are **evaluation runs, not unit tests** (CLAUDE.md §9) and are intentionally
**not imported by the live app** — a zero-reference grep here is expected.
