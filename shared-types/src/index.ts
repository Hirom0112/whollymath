// Public entry point for generated API types.
//
// Phase-0 placeholder: no Pydantic models exist yet, so there is nothing to generate.
// Once backend models land in app/api/schemas.py, run `pnpm generate` (see package.json)
// to produce ./generated.ts, then re-export it from here:
//
//   export * from './generated';
//
// One source of truth for the API contract (TECH_STACK §2).
export {};
